# INC-002: Orphaned Vercel Projects

**Date:** 2026-08-23
**Severity:** High
**Status:** Open — Owner Action Required

## Summary

Two of three Vercel projects referenced in `.vercel/project.json` files are inaccessible or deleted:
- `teora` (prj_yrkjlRtSmJrbVgWuCJgyAQHcld5b) — **DELETED** from Vercel dashboard
- `api-server` (prj_5PlXdG6841wXuah11uY8CdgMNmsn) — **EXISTS but MCP has no access** (403 Forbidden on all operations)

Only `academic-workspace` (prj_pc1NRCZYaVlZrtexnH0lkNUKKP51) is accessible via MCP.

## Root Cause

- `teora` project was deleted from the Vercel dashboard (likely manually)
- `api-server` project was created by a different session/tool and the MCP OAuth scope doesn't cover it

## Impact

1. **No backend deployed** — `api-server` Vercel project exists but is not deployed
2. **API not accessible** — No backend API endpoint is live
3. **Code not live** — `academic-workspace` deployed from old Replit Agent commit (SHA `db5e9daf88`), not from current `feat/tier-2-complete` branch
4. **No auto-deploy** — Neither project is connected to GitHub for automatic deployment

## Evidence

```
GET project prj_yrkjlRtSmJrbVgWuCJgyAQHcld5b → 404 Not Found
GET project prj_5PlXdG6841wXuah11uY8CdgMNmsn → 403 Forbidden
DEPLOY to api-server (name) → 403 Forbidden
create_git_project(api-server) → 409 Conflict (already exists)
list_projects → only academic-workspace visible
```

`academic-workspace` deployment history shows commits from Replit Agent (`actor: claude-code_2-1-226_agent`), not from current repo.

## Fix Actions

### Owner Action Required

**Option A — Via Vercel Dashboard (fastest):**
1. Go to vercel.com → `sagise-ctrl's projects` team
2. Delete the orphan `api-server` project
3. Create new `api-server` project linked to GitHub repo `sagise-ctrl/teora`, root directory `artifacts/api-server`
4. Set environment variables on the new project (same as before)
5. Trigger a deployment

**Option B — Via Vercel CLI:**
```bash
npm i -g vercel
cd artifacts/api-server
vercel login
vercel link
vercel --yes
vercel deploy --prod
```

### AI Team Actions (blocked)

- Create GitHub Actions workflow for backend deploy (blocked by no project access)
- Verify API health endpoint (blocked by no deployed backend)
- Connect projects to GitHub (blocked by no project access)

## Mitigation

- `academic-workspace` frontend is accessible and renders HTML correctly
- No runtime errors in `academic-workspace` (verified via Vercel MCP)
- Code is correct in the repo; just needs deployment

## Prevention

- Ensure all Vercel projects are linked to GitHub for auto-deploy (prevents orphan deployments)
- Document project IDs in a shared location
- Don't delete Vercel projects without updating `.vercel/project.json` references
