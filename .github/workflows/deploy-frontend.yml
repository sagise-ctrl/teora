name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "artifacts/academic-workspace/**"
      - "lib/api-client-react/**"
      - "lib/api-zod/**"
      - "lib/db/src/schema/**"
      - ".vercelignore"
      - ".github/workflows/deploy-frontend.yml"

permissions:
  contents: read

jobs:
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://academic-workspace-eta.vercel.app
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build & Deploy to Vercel
        working-directory: artifacts/academic-workspace
        env:
          VITE_SUPABASE_URL: ${{ secrets.FRONTEND_VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.FRONTEND_VITE_SUPABASE_ANON_KEY }}
          VITE_MOCK: "false"
          VITE_API_URL: "/api"
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npm install -g vercel
          echo "=== Pulling project settings (required for local build) ==="
          npx vercel pull --yes --token="$VERCEL_TOKEN" --cwd . 2>&1 | tail -5
          echo "=== Building (creates .vercel/output/) ==="
          npx vercel build --cwd . 2>&1 | tail -10
          echo "=== Verifying .vercel/output exists ==="
          ls .vercel/output/ 2>&1 | tail -3
          echo "=== Deploying prebuilt output ==="
          npx vercel deploy \
            --token="$VERCEL_TOKEN" \
            --prod --yes --prebuilt \
            --cwd=. \
            --project=academic-workspace 2>&1
          echo "=== Deployment complete ==="

      - name: Deployment summary
        run: |
          echo "Frontend deployed to Vercel production."
          echo "Verify at: https://academic-workspace-eta.vercel.app"
