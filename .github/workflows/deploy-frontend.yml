name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "artifacts/academic-workspace/**"
      - "lib/api-client-react/**"
      - "lib/api-zod/**"
      - "lib/db/src/schema/**"
      - ".vercelignore"
      - ".github/workflows/deploy-frontend.yml"

permissions:
  contents: read

jobs:
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://academic-workspace-eta.vercel.app
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps --include=optional

      - name: Build & Deploy to Vercel
        working-directory: artifacts/academic-workspace
        env:
          VITE_SUPABASE_URL: ${{ secrets.FRONTEND_VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.FRONTEND_VITE_SUPABASE_ANON_KEY }}
          VITE_MOCK: "false"
          VITE_API_URL: "/api"
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npm install -g vercel@latest
          echo "=== Pulling project settings ==="
          npx vercel pull --yes --token="$VERCEL_TOKEN" 2>&1 | tail -3
          echo "=== Installing dependencies (include optional native binaries) ==="
          # lightningcss native binary is optional — must use --include=optional in CI
          npm ci --legacy-peer-deps --include=optional 2>&1 | tail -3
          echo "=== Building ==="
          npx vite build 2>&1 | tail -15
          echo "=== Verifying dist/ ==="
          ls dist/ 2>&1 | tail -3
          echo "=== Deploying prebuilt ==="
          npx vercel deploy \
            --token="$VERCEL_TOKEN" \
            --prod --yes --prebuilt \
            --project=academic-workspace 2>&1
          echo "=== Deployment complete ==="

      - name: Deployment summary
        run: |
          echo "Frontend deployed to Vercel production."
          echo "Verify at: https://academic-workspace-eta.vercel.app"
